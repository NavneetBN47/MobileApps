{
        "select_creditcard_btn" :
        {
            "locator": { "css selector": "button[data-testid='credit_card']"}
                        
        },

        "creditcard_iframe" :
        {
            "locator": { "css selector": "#pgs-iframe"}
                        
        },

        "creditcard_number_txt" :
        {
            "locator": { "css selector": "input#txtCardNumber"}
                        
        },

        "exp_month_dropdown" :
        {
            "locator": { "css selector": "select#drpExpMonth"}
                        
        },

        "exp_year_dropdown" :
        {
            "locator": { "css selector": "select#drpExpYear"}
                        
        },

        "cvv_txt" :
        {
            "locator": { "css selector": "input#txtCVV"}
                        
        },

        "SaveCreditdetails_btn" :
        {
            "locator": { "css selector": "button#save-button"}
                        
        },

        "PaymentOptionsContinue_btn" :
        {
            "locator": { "css selector": "button[data-testid='payment-options-continue']"}
                        
        },

        "add_billing_btn":
        {
            "locator":{ "css selector": "[data-testid='add-billing']"}
        },

        "continue_btn":
        {
            "locator":{ "xpath":"//*[@data-testid='cancel-button']/following-sibling::button"}
        },

        "billing_overlay_modal":
        {
            "locator":{"css selector": ["[data-testid='billing-modal']","[data-testid='imprint-modal']"]}
        },

        "next_btn":
        {
            "locator":{"css selector":"#btn_pgs_card_add"}
        }


}